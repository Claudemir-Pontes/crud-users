# Simple CRUD to test the joi package

This project uses the Joi package to validate the form data in the back-end, while using the Prisma ORM to do CRUD operations on the database.

## Tools

- Node.js & Express;
- TypeScript;
- Joi (data validator);
- Prisma (ORM);
